name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: test (elixir ${{ matrix.elixir }} / otp ${{ matrix.otp }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        elixir: ["1.18", "1.19"]
        otp: ["27", "28"]
        exclude:
          # 1.18 needs OTP 27; 1.19 needs OTP 28
          - elixir: "1.18"
            otp: "28"
          - elixir: "1.19"
            otp: "27"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          # Note: we deliberately do NOT use `version-file: .tool-versions`
          # here. The matrix below selects the Elixir/OTP versions, and
          # `setup-beam@v1` errors out if both `version-file` and an
          # explicit `version`/`otp-version` are set (unless
          # `version-type: strict`, which would force the matrix
          # versions to be ignored). The .tool-versions file is for
          # local asdf users; CI is controlled by the matrix.
          version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      - name: Cache deps
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('**/mix.lock', '**/mix.exs') }}
          restore-keys: |
            ${{ runner.os }}-mix-${{ matrix.elixir }}-${{ matrix.otp }}-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile with warnings as errors
        run: mix compile --warnings-as-errors

      - name: Check formatting
        run: mix format --check-formatted

      - name: Run Credo
        run: mix credo --strict

      - name: Run tests
        run: mix test
